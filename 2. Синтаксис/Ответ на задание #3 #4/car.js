// Название машины не меняется
const carName = "Toyota ";

// Переменные, которые будут изменяться
let maxCarSpeed = 220;
let carOwner = "Нурислам";

// Изменяем maxCarSpeed и выводим сообщение в консоль
maxCarSpeed = 230;
console.log('У машины изменилась скорость',carName, maxCarSpeed);

maxCarSpeed = 240;
console.log('У машины изменилась скорость',carName, maxCarSpeed);

maxCarSpeed = 250;
console.log('У машины изменилась скорость',carName, maxCarSpeed);

// Изменяем carOwner и выводим сообщение в консоль
carOwner = "Алексей";
console.log('У машины изменился владелец',carName, carOwner);

carOwner = "Марина";
console.log('У машины изменился владелец',carName, carOwner);

carOwner = "Иван";
console.log('У машины изменился владелец',carName, carOwner);
