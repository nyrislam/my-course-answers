// Название машины не меняется
const carName = "Toyota";

// Переменные, которые будут изменяться
let maxCarSpeed = 220;
let carOwner = "Нурислам";

// Изменяем maxCarSpeed и выводим сообщение в консоль
maxCarSpeed = 230;
console.log('У машины изменилась скорость', maxCarSpeed);

maxCarSpeed = 240;
console.log('У машины изменилась скорость', maxCarSpeed);

maxCarSpeed = 250;
console.log('У машины изменилась скорость', maxCarSpeed);

// Изменяем carOwner и выводим сообщение в консоль
carOwner = "Алексей";
console.log('У машины изменился владелец', carOwner);

carOwner = "Марина";
console.log('У машины изменился владелец', carOwner);

carOwner = "Иван";
console.log('У машины изменился владелец', carOwner);
